# Key debounce configuration
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=3
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=3

# Encoder configuration
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# BLE transmission power configuration
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Fix battery reporting issue on Windows
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n

# OLED display configuration
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_WORK_QUEUE_DEDICATED=y

# Power management configuration (ALWAYS ON)
CONFIG_ZMK_IDLE_TIMEOUT=0
CONFIG_ZMK_SLEEP=n
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=0

# Wake-up configuration
CONFIG_ZMK_EXT_POWER=y
CONFIG_ZMK_USB=y

# ZMK Studio configuration
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n
CONFIG_ZMK_STUDIO_LOCK_ON_DISCONNECT=n

# RGB underglow configuration
CONFIG_ZMK_RGB_UNDERGLOW=y
CONFIG_WS2812_STRIP=y
CONFIG_ZMK_RGB_UNDERGLOW_EFF_START=0
CONFIG_ZMK_RGB_UNDERGLOW_BRT_MAX=30
CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER=n

# Automatically turn off RGB when idle (won’t trigger since no idle)
CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_IDLE=y

# Improve BLE connection stability
CONFIG_BT_BUF_ACL_RX_SIZE=512
CONFIG_BT_CTLR_DATA_LENGTH_MAX=251

# Optimize logging
CONFIG_ZMK_USB_LOGGING=n

# Do not clear bonded devices on startup
CONFIG_ZMK_BLE_CLEAR_BONDS_ON_START=n
